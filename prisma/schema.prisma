// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  // output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String
  phone     String?
  address   String?
  linkedin  String?
  github    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  jobs        JobData[]
  educations  Education[]
  experiences Experience[]
  skills      Skill[]
  logs        Log[]
}

model Education {
  id           String   @id @default(cuid())
  degree       String
  fieldOfStudy String
  institution  String
  startDate    DateTime
  endDate      DateTime
  userId       String
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

enum ExperienceType {
  WORK
  ORGANIZATION
  VOLUNTEER
  EVENT
}

enum ExperienceStatus {
  FULL_TIME
  PART_TIME
  INTERNSHIP
  CONTRACT
  HONORARY
  OUTSOURCED
}

model Experience {
  id          String           @id @default(cuid())
  type        ExperienceType
  status      ExperienceStatus
  title       String
  company     String
  description String?
  startDate   DateTime
  endDate     DateTime
  userId      String
  user        User             @relation(fields: [userId], references: [id], onDelete: Cascade)
}

enum SkillLevel {
  BEGINNER
  INTERMEDIATE
  EXPERT
  SPECIALIST
}

model Skill {
  id     String     @id @default(cuid())
  name   String
  level  SkillLevel
  userId String
  user   User       @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model JobData {
  id          String    @id @default(cuid())
  link        String
  jobTitle    String
  company     String
  description String?
  location    String?
  deadline    DateTime?
  createdAt   DateTime  @default(now())
  userId      String
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  results       GeneratedResult[]
  interviews    InterviewLog[]
  interviewInfo InterviewInfo?
}

model GeneratedResult {
  id          String   @id @default(cuid())
  cvText      String
  coverLetter String
  summary     String?
  createdAt   DateTime @default(now())
  jobDataId   String
  job         JobData  @relation(fields: [jobDataId], references: [id])
}

model Log {
  id        String   @id @default(cuid())
  action    String
  createdAt DateTime @default(now())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model InterviewLog {
  id        String   @id @default(cuid())
  question  String
  answer    String?
  createdAt DateTime @default(now())
  jobDataId String
  job       JobData  @relation(fields: [jobDataId], references: [id])
}

model InterviewInfo {
  id        String  @id @default(cuid())
  jobDataId String  @unique
  context   String
  feedback  String?
  score     Int?
  job       JobData @relation(fields: [jobDataId], references: [id], onDelete: Cascade)
}
